tripal:
  enabled: true
  extractor: 'svg'
  config:
    sources:
      - 'icons/*.svg'
  settings:
    size:
      title: 'Size'
      description: 'The size for this icon.'
      type: 'integer'
      default: 20
    class:
      title: 'Class'
      description: 'The class for this icon.'
      type: 'string'
      default: ''
  template: >
    <svg
      {{ attributes
        .setAttribute('viewBox', attributes.viewBox|default('0 0 24 24'))
        .setAttribute('class', class)
        .setAttribute('width', size|default('20'))
        .setAttribute('height', size|default('20'))
        .setAttribute('aria-hidden', 'true')
      }}
    >
      {{ content }}
    </svg>
