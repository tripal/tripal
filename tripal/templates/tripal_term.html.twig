{#
/**
 * @file tripal_term.html.twig
 * Default theme implementation to present Controlled Vocabulary Term data.
 *
 * This template is used when viewing Controlled Vocabulary Term pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_tripal_term()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('tripal_term') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
