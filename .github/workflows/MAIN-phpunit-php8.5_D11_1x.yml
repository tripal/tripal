name: 'PHPUnit'

on:
  workflow_dispatch:
  push:
    branches:
      - '4.x'
      - 'tv4g0-issue2367-support-php-8.5'
  # Allows us to schedule when this workflow is run.
  # This ensures we pick up any new changes in Drupal.
  schedule:
    # Run at 6am every night.
    - cron: '0 6 * * *'

permissions: read-all

jobs:
  running-tests:
    name: 'Drupal 11.1: PHP 8.5'
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'Checkout Repository'
        uses: 'actions/checkout@v4'
      - name: 'Run Automated testing'
        uses: 'tripal/test-tripal-action@v1.5'
        with:
          directory-name: 'tripal'
          modules: 'tripal tripal_biodb tripal_chado'
          php-version: '8.5'
          pgsql-version: '18'
          drupal-version: '11.1.x-dev'
          build-image: true
          phpunit-command-options: '-c /var/www/drupal/web/modules/contrib/tripal/phpunit.10.xml'
